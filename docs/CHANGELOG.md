# 변경 이력

> 설계문서 본문은 [../agent.md](../agent.md) 와 `docs/01~05`.

| 날짜 | 내용 |
| --- | --- |
| 2026-09-18 | **v3.0 — 앱 전체 구현 (M1~M5), 저장소 `lmjpt/softguard`, GitHub Actions 빌드.** 설계 검토에서 찾은 문제 4개 수정: 중복 억제가 집행을 가림 · 런처 복귀 시 오버레이 잔류 · `lastAliveAt` 유휴 오차 · 허용 시작 정각 무반응. 오버레이를 `TYPE_ACCESSIBILITY_OVERLAY` 로 바꿔 '다른 앱 위에 표시' 권한 제거. PIN 해시는 일반 SharedPreferences. R8 축소 끔(진단 불가 + 크기 제한 없음). 설계문서를 `docs/` 로 장별 분할 — 사내 프록시가 큰 API 요청을 막아 한 파일 66KB 를 올릴 수 없었기 때문이며 내용은 그대로다. 상세는 agent.md §15 |
| 2026-09-17 | 문서 생성. 요구사항 반영, Device Owner 권장안 작성 |
| 2026-09-17 | ADB 미사용 결정 → 접근성 서비스 방식으로 전면 수정 |
| 2026-09-17 | PIN 통합 / 설정앱 허용 / 우회·삭제 방지 미적용 / 임시허용 제외 반영. 권한 2종으로 축소 |
| 2026-09-17 | v1.0 설계 확정. 앱 이름·대상 기기 확정, 삼성 One UI 대응 추가 |
| 2026-09-17 | v1.1 — 정책 모델을 화이트리스트에서 블랙리스트로 전환. 보호 목록 서브시스템 제거 |
| 2026-09-17 | v1.2 — 최소 SDK를 API 33으로 상향. 앱 실행 이력 기능(R8) 추가 (모든 앱, 180일, Room) |
| 2026-09-17 | v1.3 — 이력 날짜별 필터링(R9), 중복 억제 3초→10초, 제어 중단 구간 기록(R10) 추가. 스키마에 `type`·`dayKey`·`inferred` 도입, `lastAliveAt` 기반 비정상 종료 추론, `DEVICE_BOOT` 이벤트 추가, Paging 3 제거 |
| 2026-09-17 | **v1.4 — §13 검토 항목 6개 전부 정리, 1차 범위 확정.** 중복 억제는 허용·차단 동일 10초로 확정. 앱별 집계·제어 일시 중지·CSV 내보내기는 1차 제외(§13.2 확장 후보로 이동). 차단 앱별 개별 시간대는 보류. 설정 화면에서 "제어 일시 중지" 항목 제거 |
| 2026-09-17 | **v1.5 — 개발 환경 실측 및 §14 신설.** 실기기·Android Studio 없음, JDK는 Zulu 8만 확인(AGP 8.x는 17+ 필요). Android Studio 단독 설치 권장, `:core`(순수 Kotlin/JVM) + `:app` 2모듈 구조 채택하여 기기 없이 M2 진행 가능하도록 함. 에뮬레이터로 검증 불가한 항목(One UI 절차·절전 정책·배터리 측정) 명시 |
| 2026-09-17 | v1.6 — Android Studio 설치 완료 반영(§14.1). SDK 구성요소 체크리스트 확정 |
| 2026-09-18 | **v2.0 — `:app` 모듈 추가. M1 구현 완료, 기기 테스트 대기.** 접근성 서비스 + 오버레이 차단 + 정책 파일 저장 + 4개 탭 UI(상태·차단앱·시간대·감지로그). adb 를 붙일 수 없어 **진단을 화면으로 대체**하는 것이 설계의 축이다. AGP 9 의 내장 Kotlin 지원 등 빌드 이슈를 §14.7 에, APK 크기(28MB → R8 축소 2MB)와 기기 테스트 절차를 §14.7·§14.8 에 기록 |
| 2026-09-17 | v1.9 — 정책 저장 포맷 구현으로 M2 완료. `PolicyCodec`(kotlinx.serialization) + 저장 전용 DTO 계층 추가. 버전·기본정책 검증으로 화이트리스트 시절 파일을 거부하고, 읽기 실패 시 **아무것도 차단하지 않는 방향으로 폴백**(기기가 잠기는 것을 막는다). 결정적 출력 + 골든 테스트로 포맷 고정. 테스트 60 → **84개**. §7.2 에 저장 포맷 규칙 추가 |
| 2026-09-17 | v1.8 — 프로젝트 골격 생성 및 M2 구현 완료(§14.6). Gradle 9.7.1 / Kotlin 2.4.20, `:core` 순수 Kotlin 모듈에 정책 모델·`WeeklySchedule`·`PolicyEngine`·`DayKey`·`DuplicateSuppressor` 구현, **단위 테스트 60개 전부 통과**. 사내망·VDI 환경의 빌드 차단 요인 3건(런처 JDK, 프로필 Temp 의 AF_UNIX 실패, PAC 프록시 + TLS 재서명)을 진단해 §14.5 에 기록하고 `gw.cmd` 로 우회 구성 |
| 2026-09-17 | v1.7 — SDK 설치 완료 및 개발 PC 제약 확인(§14.2). `compileSdk`/`targetSdk` = **37**, Build-Tools 36.0.0, 번들 JDK 25 확인. **개발 PC가 Xen 가상머신(중첩 가상화 미노출)이라 Android 에뮬레이터 사용 불가** → 시스템 이미지 설치 제외, `:app` 전체가 실기기 대기 상태. adb 연결도 불확실하므로 logcat 없는 진단을 전제로 **앱 내 디버그 로그 화면(개발용)** 을 요구사항에 추가하고 §10.1 상태 점검 대시보드의 중요도 상향. `:core`/`:app` 2모듈 구조를 §14.3 에 확정 |
