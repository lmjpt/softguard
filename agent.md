# 우리집 소프트가드 — 설계문서

> 상태: **v3.0 — M1~M5 구현 완료, 공기계 테스트 대기** · 갱신일: 2026-09-18
> 앱 이름: **우리집 소프트가드** · applicationId: `kr.woorijip.softguard`
> 저장소: https://github.com/lmjpt/softguard · APK: https://github.com/lmjpt/softguard/releases/latest/download/softguard.apk

가족이 쓰는 공기계(갤럭시 S20 이상, Android 13+)에 설치해, 차단 목록의 앱을 정해진 시간대에만 열리게 하고
앱 실행 이력을 날짜별로 남기는 앱. **소프트 가드** — 우회를 막는 도구가 아니라 정해둔 규칙을 기기가 지켜 주는 도구.

```
차단 목록에 넣은 앱  →  기본 차단, 공통 허용 시간대에만 열림
차단 목록에 없는 앱  →  항상 허용 (시스템 앱 포함)
모든 앱 실행 이력    →  로컬에 180일 보관, 날짜별 조회, 관리자(PIN)만
제어가 꺼졌던 구간    →  이력에 "기록 공백"으로 표시
```

## 목차

문서가 커져 장별로 나눴다 (사내 프록시가 큰 요청을 막아 한 파일로는 저장소에 올릴 수 없었다). 절 번호(§)는 원문 그대로라
`§8.7` 같은 참조는 어느 파일에 있든 같은 곳을 가리킨다.

| 파일 | 내용 |
| --- | --- |
| [docs/01-overview.md](docs/01-overview.md) | §1 개요·확정 사항·요구사항 추적 · §2 목표/비목표/막지 못하는 것 · §3 사용자 · §4 제어 방식(접근성) |
| [docs/02-architecture.md](docs/02-architecture.md) | §5 아키텍처·전력·One UI·부팅·**판정 흐름** · §6 PIN · §7 정책 모델·저장 포맷·판정 규칙·시간대 종료 |
| [docs/03-history.md](docs/03-history.md) | §8 앱 실행 이력 — 기록 대상, 중복 억제, 스키마, 파이프라인, 날짜별 조회, **제어 중단 구간**, 보관 |
| [docs/04-operations.md](docs/04-operations.md) | §9 화면 · §10 운영(상태 점검·테스트 항목) · §11 배포 · §12 마일스톤 · §13 검토 결정·확장 후보 |
| [docs/05-devenv.md](docs/05-devenv.md) | §14 개발 환경 기록 (이전 PC 기준) · **§14.8 기기 테스트 절차** |
| [docs/CHANGELOG.md](docs/CHANGELOG.md) | 변경 이력 |
| [README.md](README.md) | 받기·설치·저장소 구조·빌드 |

## 15. 구현 기록 (v3.0, 2026-09-18)

설계문서를 코드로 옮기면서 문서와 달리 한 것들. 각각 왜 그런지 적어 둔다.

### 15.1 설계 검토에서 찾아 고친 문제 4개

| # | 문제 (v2.0 문서) | 고친 것 | 어디 |
| --- | --- | --- | --- |
| 1 | §5.6 개념 코드에서 **중복 억제가 판정보다 앞**에 있었다. 차단 앱 A → 허용 앱 B → 10초 안에 A 로 돌아오면 중복으로 조기 반환되어 오버레이가 뜨지 않고 A 가 열렸다 | 집행(판정·오버레이·알람)은 **매 이벤트**, 중복 억제는 **기록에만** | `SoftGuardAccessibilityService.onAccessibilityEvent` |
| 2 | `isLoggable` 이 런처를 제외하고 조기 반환하므로, 제스처로 홈에 나가면 `overlay.hide()` 가 불리지 않아 **홈 화면 위에 차단 안내가 남았다** | 1 과 같은 수정으로 해결. 런처도 판정을 거쳐 Allowed → 오버레이 내려감. 대신 알림 창·입력기·권한 대화상자는 `isTransient` 로 상태를 건드리지 않는다 (알림 창을 내렸다고 차단 앱이 드러나면 안 됨) | 같은 곳, `AppCatalog.isTransient` |
| 3 | `lastAliveAt` 이 배치 flush 때만 갱신되는데 flush 는 이벤트가 있을 때만 일어난다. 밤새 방치 후 서비스가 죽으면 추정 종료가 몇 시간 앞으로 당겨진다. 문서의 "최대 15초 오차" 는 틀렸다 | 화면 켬/꺼짐 브로드캐스트와 접근성 이벤트 수신(1분에 한 번)에서도 갱신. 문서 §8.7 문구 수정 | `EventLogger.touchAlive`, 서비스의 `screenReceiver` |
| 4 | 허용 **종료** 알람만 있어 18:59 에 "19:00부터" 안내를 보고 있어도 19:00 에 아무 일이 없었다 | 오버레이가 떠 있을 때는 다음 허용 **시작** 시각에 알람. 정각에 재판정해 오버레이를 내린다. 알람은 여전히 1개 | `WindowAlarms`, `enforce()` |

### 15.2 단순화

| 항목 | 문서 | 구현 | 이유 |
| --- | --- | --- | --- |
| 오버레이 권한 | `SYSTEM_ALERT_WINDOW` 필요 | **불필요.** `TYPE_ACCESSIBILITY_OVERLAY` 창은 접근성 서비스가 권한 없이 띄울 수 있다 | 필요 권한 2종 → 1종. 설치 절차 한 단계 감소. 폴백(홈 호출)은 창 생성이 예외로 실패할 때만 |
| PIN 저장 | `EncryptedSharedPreferences` | 일반 `SharedPreferences` | 저장값이 이미 salt 를 섞은 PBKDF2 해시. 그 라이브러리는 폐기됨. `allowBackup=false` 로 파일 반출 경로도 없음 |
| 정책 저장 | DataStore | `filesDir/policy.json` 직접 읽고 쓰기 (임시 파일 → rename) | 문서 안에서도 DataStore 와 파일이 섞여 있었다. 포맷은 `PolicyCodec` 이 정하므로 저장 매체는 단순한 쪽으로 |
| 오버레이 뷰 | (미정) | Compose 대신 일반 `View` | 서비스 컨텍스트에서 라이프사이클 소유자 없이 안정적으로 붙이기 위해 |
| R8 축소 | 필수 (메일 25MB 한도) | **끔** | APK 를 GitHub 에서 받으므로 크기 제한이 없고, 기기에서 logcat 을 볼 수 없어 R8 이 만드는 문제를 진단할 방법이 없다. keep 규칙은 `proguard-rules.pro` 에 남겨 두었다 |
| 차단 목록 제외 | 자기 자신·기본 홈·시스템UI | + **설정 앱** | 확정 사항 표의 "설정 앱 차단하지 않음" 을 UI 에서도 강제. 설정을 막으면 접근성을 끌 수도 없어 되돌리기 어렵다 |
| PIN 재요청 | 앱을 열 때마다 | 앱을 떠난 뒤 **30초**가 지나면 | 설정 화면을 오가며 권한을 켜는 동안 매번 PIN 을 치게 되는 문제. 아이가 그 30초 안에 폰을 집어드는 경우는 감수 |

### 15.3 빌드·배포 경로

이 PC 에는 Android SDK·JDK 17·Gradle 이 없다. **소스는 GitHub API 로 올리고(회사 프록시는 큰 요청만 막는다), APK 는 GitHub Actions 가 만들고, 폰은 Release 에서 받는다.** 가족 앱(family-hub)과 같은 방식이다. 검증된 조합: AGP 8.7.3 · Kotlin 2.0.21 · Gradle 8.11.1 · compileSdk 35 · Compose BOM 2024.12.01 · Room 2.6.1 (KSP).

- `:core` 단위 테스트가 매 빌드마다 돈다 — `PolicyCodec` 골든 테스트, `WeeklySchedule` 경계, 판정 5단계, 중복 억제, `DayKey`, 공백 추론, 타임라인
- 릴리스 서명 키: `android-signing.local/keystore.p12` (git 제외). 지문 SHA256 `C5:23:A8:31:…:8C:40`. **잃으면 기존 설치본 위에 업데이트 불가**

### 15.4 실기기에서 반드시 확인할 것 (문서 §14.8 에 추가)

- `TYPE_ACCESSIBILITY_OVERLAY` 창이 One UI 에서 전체 화면을 덮는지, 상태바를 내렸다 올려도 유지되는지
- 뒤로 가기가 오버레이를 뚫고 아래 앱으로 가는지 (의도: 앱이 닫히면 홈 이벤트가 와서 오버레이도 내려감)
- 차단 앱 → 다른 앱 → 즉시 차단 앱 복귀에서 오버레이가 매번 뜨는지 (15.1 #1)
- 홈 제스처로 나갔을 때 오버레이가 남지 않는지 (15.1 #2)
- 허용 시작 정각에 덮여 있던 앱이 열리는지 (15.1 #4)
- 접근성을 끄면 알림 "제어가 꺼졌어요" 가 오는지, 이력에 공백 블록이 생기는지
